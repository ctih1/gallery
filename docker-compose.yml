services:
    web:
        build: .
        env_file:
            - .env
        ports:
            - "3001:3000"
        volumes:
            - thermal:/sys/class/thermal:ro
        environment:
            NODE_ENV: production
        restart: unless-stopped
        extra_hosts:
            - "host.docker.internal:host-gateway"
